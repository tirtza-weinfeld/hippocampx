```python meta="source=problems/121-best-time-to-buy-and-sell-stock/solution.py"
def best_time_to_buy_and_sell_stock(prices: list[int]) -> int:
    cost, profit = float("inf"), 0
    for price in prices:
        cost = min(cost, price)
        profit = max(profit, price - cost)
    return profit
```