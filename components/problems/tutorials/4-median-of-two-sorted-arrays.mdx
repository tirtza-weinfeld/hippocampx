import { MedianOfTwoSortedArraysIllustration } from '@/components/illustrations/problems'

# Median of Two Sorted Arrays

## Resizable Table Of contents

<DifficultyBadge difficulty="hard" />

<PillList pills={["binary-search"]} />

[4. Median of Two Sorted Arrays](https://leetcode.com/problems/median-of-two-sorted-arrays/description/)

## [!(ProblemDefinition)] Definition

Given two individually sorted arrays nums1 (size m) and nums2 (size n), compute the median of their combined multiset without merging them, in total time O(\log(m+n)).

<MedianOfTwoSortedArraysIllustration size="full" />

## [!(ProblemIntuition)] Intuition

- Binary-search how many elements to take from the smaller array:
    - so that, together with the forced number from the other array,            they form a globally ordered left half whose boundary determines the median.

- We split each array at index `i` (for `a`) and `j` (for `b`):
    ```
    a:  a[:i] | a[i:]
    b:  b[:j] | b[j:]
    ```
    - Now we only need the **largest value on the left** and the **smallest value on the right**:
        - `al = a[i-1]` → last element of `a`’s left side
            - If `i == 0`, left side is empty → use `-∞`
        - `ar = a[i]` → first element of `a`’s right side
            - If `i == m`, right side is empty → use `+∞`
        - Same logic for `b`:
            - `bl = b[j-1]` → max of `b`’s left
            - `br = b[j]` → min of `b`’s right
        - The infinities make comparisons like `al <= br and bl <= ar`  work even when one side is empty.

- Valid partition condition:
        - boundaries are ordered -- `all left <= all right`
        - `al <= br`  and  `bl <= ar`

- Once valid:
        - If total is odd: `median = max(al, bl)`   (the last element of the left side)
        - If total is even: `median = (max(al, bl) + min(ar, br)) / 2`

## [!(ProblemTimeComplexity)] Time Complexity

- $O(log(min(m, n)))$
    - Binary search is performed on the shorter array.

## [!(ProblemCodeSnippet)] Code Snippet

```python meta="source=problems/4-median-of-two-sorted-arrays/solution.py"
class Solution:
    def findMedianSortedArrays(self, a: list[int], b: list[int]) -> float:
        if len(a) > len(b): a, b = b, a
        m, n, low, high = len(a), len(b), 0, len(a)

        while low <= high:
            i = (low + high) // 2
            j = (m + n + 1) // 2 - i

            al = a[i-1] if i > 0 else float('-inf')
            ar = a[i] if i < m else float('inf')
            bl = b[j-1] if j > 0 else float('-inf')
            br = b[j] if j < n else float('inf')

            if al <= br and bl <= ar:
                if (m + n) % 2: return float(max(al, bl))
                return (max(al, bl) + min(ar, br)) / 2.0

            if al > br: high = i - 1
            else: low = i + 1
```

